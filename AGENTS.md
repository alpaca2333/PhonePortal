# Agent 协作规则（AGENTS.md）

> 本文件是**强制约定**，对所有在此仓库工作的 Agent / 开发者生效。动工前先读 [docs/TECHNICAL.md](docs/TECHNICAL.md)。

## 核心规则：决策必须落文档

**任何值得落地的技术与设计决策、以及后续的任何变更，都必须同步落地到文档中。** 不写文档的改动视为未完成。

需要记录的内容（不限于）：
- **选型**：用了什么库/框架/方案、为什么选它、否决了哪些替代、代价是什么。
- **架构**：模块划分、数据流、文件职责、关键不变量。
- **坑**：真实踩过的坑与规避方式（尤其「为什么不能那样做」）。
- **接口/约定**：manifest 字段、API、资产格式、命名约定、环境假设。
- **验证与限制**：如何证明它是对的、已知还差什么。

## 文档写到哪里

| 内容 | 位置 |
|---|---|
| 门户整体架构 / 构建 / 后端 / 坑 | [docs/TECHNICAL.md](docs/TECHNICAL.md) + 根 [README.md](README.md) |
| 单个子应用的架构、决策、资源、扩展方式 | 该应用自己的 `apps/<id>/README.md` |
| 跨应用的平台级规则 | 本文件 `AGENTS.md` |

## 落地要求

1. 新增/改动子应用 → 维护 `apps/<id>/README.md`，并在 `docs/TECHNICAL.md` 与根 `README.md` 的应用清单里补/改对应行。
2. 改动构建管线、后端、门户外壳 → 同步更新 `docs/TECHNICAL.md` 对应章节。
3. 记录「为什么」而不只是「是什么」—— 后来者最需要的是决策理由与踩坑记录。
4. 引入外部资源（模型/字体/图标/库）→ 写清来源、许可证、体积、获取方式、降级方案。

## 启动与验证（强制）

1. **起门户一律用 `npm run dev`（watch 模式）**：它自己完成「构建 → 启动 → 每 800ms 轮询 `server/ apps/ shell/ shared/ scripts/` 的 mtime 签名 → 变了就重建 + 重启」（**不要改回 `fs.watch`**：本机递归 watcher 会静默失效，见 `docs/TECHNICAL.md` 第 5 节）。**不要**用 `npm start` / `npm run launch` 起长期服务——那是启动时把 `dist/**/*.js` 载入内存的普通进程，改完 `server/`、`shared/` 的代码不会生效（真实踩过：`normalizeManifest` 加了字段、`/api/manifest` 却没有，因为跑着旧模块）。
2. **改完代码不要手动 `npm run build` + 重启**：watcher 会做。只有「验证构建本身」或「没有 watcher 时」才手动 build；构建是原子的（`dist.next/` → `dist/`），失败不会毁掉正在服务的 `dist/`。
3. **验证要用具体 URL**：`curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/...`，改了什么就验什么（例如改了 manifest 字段就 `curl /api/manifest` 并 grep 该字段）。
4. **不要在别的端口另起一个服务来做验证**：那会和主服务抢文件、留下残留进程。必须临时起时用 `PORT=<空闲端口>` + 后台任务，并在结束前 kill 掉，再确认端口已释放（`curl` 返回 000）、`ps -ef | grep dist/server` 无残留。
5. 本环境**没有浏览器**：WebGL / 触摸手感 / 屏幕方向锁定这类只能真机验证的，写完要**明确说明「需真机确认」**，并把 CPU 侧能验证的部分（模拟、投影、矩阵）用 node 脚本验证并记录结果。
6. **验证按改动范围跑，不默认跑全套**：`scripts/verify-*.mjs` 是**回归门禁**，不是「每次改动都全跑一遍」的仪式——这些脚本里有解析真实 GLB 的、有必须 `--expose-gc` 的、还有按实测分布定带宽的性能断言，跑无关脚本只换来等待和噪声（真实教训：把「顺手全跑」和手动 build 写进同一条命令，撞上 watcher 的构建，见第 2 条）。默认做法：
   - **跑钉住你改动那个模块的脚本**——映射表属于各应用自己（射击子应用见 `apps/shooter/README.md` 的「改动 → 跑哪个脚本」表；`scripts/<脚本>.mjs` 头部注释也写着它证明什么）；
   - 按第 3 条 `curl` 具体 URL、按第 5 条写明真机确认项（这两条**每次都要做**，只有「跑脚本」这一步是按范围来的）；
   - **只有这三种情况才跑全套**：① **改了某个脚本的断言数量**（各应用 README 记着「N 项 / M 个脚本 / 全部 0 失败」这类**全局声明**，动了计数就必须重新跑出来，不许照抄或估算）；② **动了平台层**（`server/`、`shared/`、`shell/`、`scripts/`，含 `scripts/build.mjs`）；③ 用户明确要求。
   - 无论跑几个脚本，**第 2 条都无条件生效**：先确认有没有 watcher 在跑，有就**绝不**手动 `npm run build`（两个构建同时在换目录会把 `dist/` 留成残缺树，症状与恢复步骤见 `docs/TECHNICAL.md`）。

## 设置与用户数据（强制）

1. **所有设置项必须持久化到服务器**，走 `GET/PUT /api/settings/:scope`（落盘 `data/settings.json`）。
   **禁止**把设置只写进 `localStorage`：同一浏览器的外壳与所有 iframe 虽然共享 localStorage，但清站点数据 / 换设备 / 重装都会静默丢失，而且服务器侧完全看不到这份状态。
2. `localStorage` 只允许用于**应用内容**（如 `apps/notes` 的笔记正文）或**纯瞬时 UI 状态**；判断标准是「用户会不会认为这是他的设置」——会，就必须上服务器。
3. 存储位置必须在 `dist/` **之外**：`npm run build` 会整体换掉 `dist/`（`rm dist` + `rename dist.next dist`），放进去的数据每次保存都会丢。当前统一放 `data/`（已 gitignore）。
4. 新增设置项时**必须同时**：
   - 在 `apps/<id>/src/settings.ts`（或该应用对应模块）里定义键名、范围、默认值与钳制规则（默认值要与现有硬编码表现一致，避免「升级后手感变了」）；
   - **按分组组织**：键路径是 `scope.<组>.<方向>.<键>`（如 `shooter.stick.landscape.sizePx`、`shooter.camera.portrait.heightScale`），每个分组有自己的「恢复默认」且只清自己那组；设置项多了就加组，不要再往同一层堆；
   - 需要影响渲染/引擎状态（不是 CSS 变量）的设置，用**回调**交给组装层（`main.ts`）转给对应模块，**不要让设置面板 import 渲染模块**；
   - 在 `apps/<id>/README.md` 写清键名、范围、默认值、生效时机、失败降级；
   - 补 CPU 侧断言（`scripts/verify-stick.mjs` 这类纯逻辑脚本；面板接线用 `scripts/verify-panel.mjs` 的 DOM shim），并确认刷新页面 / 重启服务器 / 重新构建后设置仍然生效（用 `curl` 验证并记录）。
5. **稀疏覆盖**：只存用户改过的键，未改的键继续跟随代码里的默认值（默认值可随视口/环境变化）。保存时提交客户端内存中的完整 scope 对象，并**原样保留自己不认识的其他键**，否则旧客户端会抹掉新版本写入的设置。
6. 服务端只做「JSON 结构 + 体积 + 键名」的通用校验，**不校验业务 schema**；业务 schema 由各应用自己的 `settings.ts` 负责，读到脏数据必须钳制/丢弃而不是抛错。

## 子应用之间传文件：外部资产目录（强制）

子应用之间**不互相 import、不互相写文件**；要传文件（今天是「FBX→GLB 转换器把产物交给游戏」）只有一条通道：

1. **落在 `data/assets/<接收方 appId>/<名字>`**，**不在 `apps/` 下、不在 `dist/` 下、不入库**。理由三条都要记住：① `apps/` 被 dev 监督者轮询，放那里等于**每次发布都重建 + 重启服务器**；② `build.mjs` 会把 `apps/` 拷进 `dist/`，大文件每次构建都被复制；③ 发布是运行时用户内容，写进源码树会弄脏工作区，而 GitHub **单文件 >100MB 直接拒推**。判断标准与设置同一条：运行时被写、体积可能大、内容属于用户 → 一律 `data/`（已 gitignore，且不在轮询范围）。
2. **接收方必须在自己的 `manifest.json` 里声明** `"assets": { "accepts": ["glb"] }`。没声明 = 服务端 415 拒收，发布端也不会把它列为目标。**加一个能收资产的子应用只需改它自己的 manifest**，不许为此改服务器或发布端。
3. **写入 API**：`PUT /api/assets/<appId>/<name>`（裸 body）；**读取走逻辑 URL** `/assets/<appId>/<name>`（磁盘路径 ≠ URL，这样以后换存储布局不用动游戏）；列表 `GET /api/assets/<appId>`；删除 `DELETE` 同名路径。
4. **上传实现必须满足三条**（`server/src/assets.ts` 是样板）：**流式落盘**（上限默认 256MB，`PORTAL_MAX_ASSET_MB` 可覆盖）、**校验通过才 `rename()` 原子替换**（失败的上传绝不能毁掉已发布的那个文件）、**临时文件用隐藏名**（列表/删除不会看到它）。新增接收类型（png 等）时必须给它自己的校验器，不能默认相信字节。
5. **发布是显式动作**：「转换/预览」这类本地路径不许联网；请求一律**相对路径**（`/api/...`），源码里不允许出现绝对 URL。发布功能的文档要写清「哪些操作不联网、哪些会写服务器」，别让「不上传」变成一句假话。
6. 改动这一层 → 跑 `node scripts/verify-assets.mjs`（起真实服务器 + 临时 `data/`），并在 `docs/TECHNICAL.md` 第 4 节与**接收方**的 `apps/<id>/README.md` 里维护约定（写清目录、URL、manifest 字段、失败模式）。

## 子应用对外提供 API（强制）

一个子应用可以给**别的子应用或脚本**提供 HTTP API（今天是 `apps/fbx2glb` 的 `POST /api/fbx2glb/convert`：
FBX 进、GLB 出）。规矩：

1. **只走 HTTP**。子应用之间**不许互相 import、不许共享模块**（跨应用通道 = 目录 + HTTP）。API 的实现放
   `server/src/<appId>.ts`（HTTP 层）与 `server/src/<appId>Worker.ts`（执行层），**执行层里 import 该子应用
   自己的 `src/*.js`**，绝不重写一遍业务逻辑（两份实现必然漂）。
2. **重量级/可能崩的活放 worker 线程**：主线程要一直能服务门户与游戏；一次 200MB 的解析、或者把 three 的
   堆搞炸，都只能影响那一个请求。跑完就 `terminate()`。
3. **资源必须有闸门**：上传**流式落盘**（不缓冲）+ 体积上限（环境变量可调）、**并发 1**、**超时并 kill**、
   临时文件放 `data/tmp/` 且**任何路径都要删干净**（含失败路径）。
4. **参数校验要严、业务钳制要交给业务模块**：未知参数一律 400（typo 不能被静默忽略）；数值范围由该应用自己的
   `settings.ts` 钳制（与页面同规则），实际生效值写进响应头。
5. **说真话**：做不到的事（如服务端没有图片解码器 ⇒ 不支持贴图）要在**响应头/错误信息**与文档里写明，
   不许静默降级成一个看起来对、其实是坏的文件。响应头只放 ASCII（中文 URI 编码）。
6. **可发现**：提供一个 `GET /api/<appId>` 自描述端点，并用验证脚本断言它的参数列表与解析器接受的集合**完全一致**；
   补 `scripts/verify-*.mjs` 断言（成功路径 + 全部失败路径 + 不留临时文件），并在**调用方**的 `apps/<id>/README.md`
   里记下怎么调（写清方法、参数、状态码、上限与已知限制）。

## 子应用 README 最小结构

1. 一句话简介 + 如何运行/操作
2. 架构与模块职责
3. 关键技术决策（含被否决的替代方案）
4. 外部资源与许可证
5. 已知限制 / 待办
6. 如何扩展
